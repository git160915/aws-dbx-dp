# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info
*.lock.hcl

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore Python-specific files and virtual environments
# Python generates cache and compiled files, and virtual environments should not be committed.
__pycache__/  # Byte-compiled cache directory
*.py[cod]  # Compiled Python files (.pyc, .pyo, .pyc)
*.so  # Shared object files generated from C extensions
*.egg-info/  # Metadata directory for Python packages
.Python  # Environment configuration file
env/  # Virtual environment directory
venv/  # Virtual environment directory (alternative name)

# Ignore CloudFormation templates and outputs
# CloudFormation templates and outputs can be generated dynamically and should be excluded.
*.template  # JSON or YAML formatted CloudFormation templates
*.json  # Generic JSON files (ensure this does not exclude necessary files)
*.yaml  # YAML format files
*.yml  # Alternative YAML format
.stacksets/  # Stack set configurations
.cfn-outputs.json  # JSON outputs from CloudFormation deployments

# Ignore Docker-related files
# Docker generates temporary and log files that should not be included in version control.
.dockerignore  # Docker ignore file, similar to .gitignore
.docker  # Local Docker metadata directory
*.tar  # Docker image archives
*.log  # Log files generated by Docker
docker-compose.override.yml  # Override configuration for Docker Compose
docker-compose*.yml  # All Docker Compose configuration files

# Ignore dbt (Data Build Tool) related files
# dbt generates compiled SQL files, logs, and other artifacts that should not be committed.
target/  # Compiled SQL and other build artifacts
logs/  # Logs generated during dbt runs
dbt_packages/  # Installed dbt packages
manifest.json  # dbt project manifest file
run_results.json  # Stores metadata about the last run

# Ignore keys
*.pem

.DS_Store*

*.deb
